---
status: solved
---

Solution by **anakinwang**.
***

We claim the only solution is $f(x) \equiv x$.

Proof. Let $P(x, y)$ denote the problem assertion. It is clear $f(x) \equiv x$ satisfies $P$.

To show this is the only solution, observe $P(0, y): f(0) = f(0)f(y)$ for all $y \in \mathbb{R}$, implying either $f(0) = 0$ or $f(y) = 1$ for all $y$. The latter implies $1 = x^2 + 1$ for all $x \in \mathbb{R}$, an absurdity.

As such, $f(0) = 0$; we have $P(x, 0): f(x^2) = x^2$ for all $x \in \mathbb{R}$, i.e., $f(x) = x$ for all $x \geq 0$. Now, fix some $x < 0$, and pick some $y \in (0, -x)$. Since $x^2 + xy > 0$, it follows that $x^2 + xy = f(x^2 + xy) = x^2 + f(x)y$, so $f(x) = x$ for all $x \in \mathbb{R}$, as desired.
