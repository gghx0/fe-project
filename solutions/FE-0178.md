---
status: solved
---

Solution by **RagvaloD**.
***
$y=0: f(x^2)=x^2+f(0)$, so for $x\geq 0 $ we have $f(x)=x+c$, where $c$ is some const.
And for $x<0$ we can define any value of $f(x)$ because function behaviour is not defined for negative values.