---
status: solved
---

Solution by **benjaminchew13**.
***

Let $P(x, y)$ denote $f(f(x)^{2} + f(xy)) = f(x^{2}) + xy$.

$P(1, y) : f(f(y) + f(1)^{2}) = y + f(1)$, so we have that $f$ is bijective.

$P(0, 0) : f(0)^{2} + f(0) = 0$, so $f(0) \in \{ 0, -1 \}$.

$P(x, 0) : f(x)^{2} = x^{2} - f(0)$.

$f(f(x)^{2} + f(xy)) = f(x^{2}) + xy$

$f(x^{2} - f(0) + f(xy)) = f(x^{2}) + xy$

$f(x^{2} - f(0) + f(xy))^{2} = f(x^{2})^{2} + 2xyf(x^{2}) + x^{2}y^{2}$

$-x^{2}f(0) - f(0) + (x^{2} - f(0))f(xy) = xyf(x^{2})$
***
Case 1 : $f(0) = -1$

Note that we have $f(x)^{2} = x^{2} + 1$ for all $x$.

$(x^{2} + 1)(f(xy) + 1) = xyf(x^{2})$

$(x^{2} + 1)^{2}(f(xy)^{2} + 2f(xy) + 1) = x^{2}y^{2}f(x^{2})^{2}$

$f(xy) = -\frac{x^{4}y^{2} + x^{4} + 2x^{2} + 1}{(x^{2} + 1)^{2}}$

If we take $y \to \frac{y}{x}$ and $x \neq 0$, we have $f(y) + 1 = -\frac{x^{2}y^{2}}{(x^{2} + 1)^{2}}$ for all $x \neq 0$.

Then if we take $x = 1$ we have $f(y) + 1 = -\frac{y^{2}}{4}$, so $(x^{2} - 1)^{2}y^{2} = 0$ for all $x \neq 0$ and $y$, which is obviously false.

So there are no solutions for this case.
***

Case 2 : $f(0) = 0$.

We have $f(x)^{2} =x^{2}$ for all $x$.

Then we have $x^{2}f(xy) = xyf(x^{2})$ for all $x$ and $y$.

$x= 1$ gives $f(y) = yf(1)$ for all $y$, and since $f(1)^{2} = 1$, we have $f(1) = 1$ or $f(1) = -1$.

So $f(x) \equiv x$ and $f(x) \equiv -x$, and checking gives that both of them are solutions.


So the only solutions are $f(x) \equiv x$ and $f(x) \equiv -x$.
